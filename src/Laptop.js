import React from 'react';
import {
    useParams,
    useNavigate,
    useLocation,
  } from "react-router-dom";

export default function Laptop() {
    let navigate = useNavigate();
    return (
        <div>
            <p>laptop</p>
        </div>
    )
}